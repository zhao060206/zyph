# Phototropic 摄影网站 · 完整部署方案

> 照做即可，全程约 8 分钟。不需要编程知识。

---

## 一、这套方案长什么样

```
你的电脑
  └─ 7 个文件 ──拖拽上传──▶ GitHub 仓库（存代码）
                                │ 自动侦测更新
                                ▼
                          Render 云端服务器（跑网站）
                                │ 读写数据
                                ▼
                     腾讯云 COS（存照片 + 点赞评论）
```

**三者各管什么：**

| 平台 | 免费吗 | 负责什么 |
|---|---|---|
| **GitHub** | 免费 | 存放代码。以后改网站就是替换这里的文件 |
| **Render** | 免费 | 把网站跑起来，提供公网地址给访客访问 |
| **腾讯云 COS** | 你已有 | 存照片、存点赞评论数据（数据不会因 Redeploy 丢失） |

---

## 二、要准备的东西

**1. 一个 GitHub 账号** — 没有就现注册，免费

**2. 一个 Render 账号** — 用 GitHub 账号一键登录即可，免费

**3. 腾讯云的两把钥匙**（SecretId 和 SecretKey）

获取位置：登录腾讯云 → 右上角**头像** → **访问管理** → 左侧**访问密钥** → **API 密钥管理**

- **SecretId**：`AKID` 开头的一长串
- **SecretKey**：点「显示」后可见

> ⚠️ **这两把钥匙等同于你 COS 的账号密码，不要发到聊天里、不要贴在公开网页上。**
> 在下面的步骤里，你只需要把它们填进 Render 后台的"环境变量"输入框——那相当于放进服务器的保险箱，网页代码里看不到。
>
> 另外：建议在腾讯云给这把密钥**只授予 COS 权限**（访问管理里可以设置策略），万一泄露损失也可控。

---

## 三、第一步：上传到 GitHub

**1.** 打开 https://github.com/new

**2.** 填写：
- **Repository name**：`phototropic`
- 选 **Public** 或 **Private** 都行（Private 也一样能用）
- **不要**勾选 "Add a README file"
- 点绿色按钮 **Create repository**

**3.** 在新页面找到 **uploading an existing file** 这个链接（蓝色字），点它

**4.** 打开电脑上的 `phototropic-live` 文件夹，把里面这 **7 个文件**全选，拖进网页的上传框：

```
index.html          ← 网站页面本体
server.js           ← 后台服务
package.json        ← 依赖清单
package-lock.json   ← 版本锁定
Dockerfile          ← 部署配置
.dockerignore       ← 打包忽略清单
部署说明书.md        ← 本文档
```

> ⚠️ **两个注意点**：
> 1. 拖**文件本身**，不要拖整个文件夹
> 2. 如果看到 `node_modules` 文件夹，**不要上传它**（Render 会自己装）
> 3. `.dockerignore` 是隐藏文件，如果文件名是 `.dockerignore` 开头带点，Windows 可能默认不显示——在文件夹选项里勾选"显示隐藏文件"就能看到

**5.** 拉到页面底部，点绿色 **Commit changes**

---

## 四、第二步：在 Render 建站

**1.** 打开 https://dashboard.render.com/ ，点 **Sign in with GitHub** 授权登录

**2.** 点右上角 **New +** → 选 **Web Service**

> ⚠️ **一定要选 Web Service**，不要选 Static Site
> （因为摄影网站现在有后台服务，Static Site 跑不了 server.js，点赞评论会失效）

**3.** 在仓库列表里找到 `phototropic`，点 **Connect**

**4.** 填写配置：

| 项目 | 填什么 |
|---|---|
| **Name** | `phototropic`（决定网址，会变成 `phototropic.onrender.com`） |
| **Region** | **Singapore**（离国内最近） |
| **Branch** | `main` |
| **Instance Type** | **Free** |

**Build 和 Start 命令**：因为文件夹里有 `Dockerfile`，Render 会自动识别并把 Runtime 显示为 **Docker**，这两栏会变灰不用填。

> 如果它显示 Runtime 是 Node（没识别出 Docker），就手动填：
> - Build Command：`npm install`
> - Start Command：`node server.js`

**5.** **⭐ 最关键的这一步**：往下滚到 **Environment Variables**，点 **Add Environment Variable**，添加这 3 条：

| Key | Value | 从哪来 |
|---|---|---|
| `COS_SECRET_ID` | 你的 SecretId | 腾讯云访问密钥管理 |
| `COS_SECRET_KEY` | 你的 SecretKey | 同上 |
| `COS_BUCKET` | `zhao-1465501838` | 这是你的存储桶名，直接照抄 |

> 三条都要填，一个字母都不能错。填错了点赞评论能看但不能存。

**6.** 点 **Create Web Service**，等 2~3 分钟构建

**7.** 构建完成后，页面顶部会出现你的网址：
### `https://phototropic.onrender.com`

---

## 五、第三步：验收（重要，一定要做）

**1. 检查后端是否正常**

在网址后面加 `/api/health` 访问，例如：
`https://phototropic.onrender.com/api/health`

应该看到这样的内容：
```json
{"ok":true,"loaded":true,"storage":"cos","cosConfigured":true,"photos":0}
```

逐项对照：

| 字段 | 应该是 | 如果不是 |
|---|---|---|
| `ok` | `true` | 服务没起来，看 Render 的 Logs 标签页 |
| `cosConfigured` | `true` | **环境变量没填对**，回 Render 检查那 3 条 |
| `storage` | `"cos"` | 同上，现在在用临时存储，数据会丢 |

**2. 检查网站功能**

打开主页，逐个测试：

- [ ] 能看到照片（照片一直存在 COS 里，应该立刻可见）
- [ ] 点开一张大图，点心形 ❤️ → **立刻变红**
- [ ] 输入框写句话回车 → 评论出现在列表里
- [ ] 点「改名字」→ 能改名字、能从 12 个头像里挑

**3. 检查数据真的存下来了**

刷新页面，看点赞和评论**还在不在**。
还在 = 数据成功写入 COS ✓
消失了 = 环境变量有问题，回第 4 步检查

---

## 六、第四步：防止网站"打瞌睡"

Render 免费版有个机制：**连续 15 分钟没人访问就休眠**，下一个访客打开要等 30~50 秒。
用这个免费工具解决：

**1.** 打开 https://uptimerobot.com → 点右上角 **Register for FREE** 注册（填邮箱密码，去邮箱点验证链接）

**2.** 登录后点左上角 **+ Add New Monitor**

**3.** 只填三处：
- **Monitor Type**：选 `HTTP(s)`
- **Friendly Name**：随便填，比如 `我的摄影站`
- **URL**：填你的网站地址 `https://phototropic.onrender.com`
- **Monitoring Interval**：保持默认 `5 minutes`

**4.** 点右下角绿色 **Create Monitor**，完成

它会每 5 分钟自动访问一次你的网站，让 Render 以为一直有人来，就不会休眠。

---

## 七、以后怎么用

### 上传新照片 —— 不需要动代码！

1. 打开你的网站，在网址末尾加 `#admin`，例如：
   `https://phototropic.onrender.com/#admin`
   （或者在左上角 "Phototropic" 字样上快速连点 5 次）

2. 站长面板有 **4 个标签页**：

   | 标签 | 用途 |
   |---|---|
   | **上传新照片** | 选照片 → 填日期标题 → 点上传。相机参数自动识别 |
   | **管理已有** | 删除、替换照片，修改照片介绍 |
   | **背景音乐** | 上传 mp3，访客页面右下角出现黑胶唱片 |
   | **站点设置** | 填腾讯云密钥、备份页面源码 |

3. 第一次打开会自动跳到「站点设置」让你填密钥（只需填一次，之后浏览器会记住）

   > 这里的密钥填的是**同一个** SecretId / SecretKey。
   > 为什么两处都要填？
   > - **Render 环境变量**：是给**服务器**用的，负责访客的点赞评论能存下来
   > - **这里（浏览器）**：是给你**上传照片**用的，密钥只存在你自己电脑上
   >
   > 两者互不影响，都要填。

### 修改网站页面（改文字、颜色、布局）

1. 打开 GitHub 仓库 → 点 `index.html` → 右上角**铅笔图标**
2. 改完拉到最底点 **Commit changes**
3. Render 自动重新部署，约 1~2 分钟生效

### 想在本机先试试（可选）

双击 `phototropic-live` 文件夹，在此目录打开命令行运行：
```
node server.js
```
然后浏览器打开 `http://localhost:3000`

---

## 八、常见问题

**Q：为什么不用 CodeSandbox 了？**
那个每次打开会先弹"是否继续预览"确认页，而且匿名沙箱没有长期保障。Render 地址固定、打开直达、无中间页。

**Q：点赞评论的数据存在哪？会丢吗？**
存在你 COS 的 `interactions.json` 文件里。Render 重新部署 100 次也不影响它——数据一直在腾讯云上。你可以登录 COS 控制台直接看到这个文件。

**Q：照片存在哪？**
照片还是原来的位置（COS 的 `photos/` 目录），这次没有搬动。

**Q：网页代码里能看到我的密钥吗？**
看不到。网页源码里只有存储桶名字，没有任何密钥。密钥只存在两处：Render 的环境变量（服务器）和你的浏览器本地。

**Q：网站打开有点慢？**
首次打开（休眠唤醒后）需要 30~50 秒，这是 Render 免费版的机制，配了 UptimeRobot 就不会遇到。正常访问时很快——页面只有 148KB，而且已经做了性能优化（详见下节）。

**Q：怎么知道有没有人在用我的网站？**
访问 `你的网址/api/health`，看 `photos` 字段的数字 = 有多少张照片有互动记录。

**Q：有人发垃圾评论怎么办？**
目前评论只能由发表者本人删除。如果你需要以站长身份删除任意评论，可以在 Render 环境变量里加一条 `ADMIN_TOKEN`（自己设一个密码），然后告诉我，我给你加一个删除入口。

**Q：访问量大了会超免费额度吗？**
Render 免费版每月 750 小时（够一个站点全天运行），不限流量。腾讯云 COS 有免费额度，具体看你的套餐。

---

## 九、本次做的性能优化（你知道就行）

网站从约 300KB 瘦到 148KB，主要靠这几项：

| 优化 | 效果 |
|---|---|
| **COS SDK 改为按需加载** | 这个 SDK 有 **156KB**，但只有你上传照片时才用得到，访客完全不需要 → 改成打开管理面板时才下载。**首屏少下载 156KB** |
| 转义函数改用查表 | 原来每次转义文字都创建一个隐藏元素，现在纯字符串替换，实测 1 万次只要 5.6 毫秒 |
| 头像图形加缓存 | 12 款头像的图形只生成一次，评论列表刷新时不再重复计算 |
| 渲染加变更检测 | 数据没变化时跳过界面重绘，切图更顺滑 |
| 清理废弃代码 | 删掉了失效的 Supabase 相关配置 |

---

## 十、总检查清单

打印出来或对着勾：

**部署阶段**
- [ ] GitHub 建好仓库，7 个文件上传成功
- [ ] Render 建的是 **Web Service**（不是 Static Site）
- [ ] 3 个环境变量都填了（`COS_SECRET_ID` / `COS_SECRET_KEY` / `COS_BUCKET`）
- [ ] `/api/health` 返回 `cosConfigured: true`

**验收阶段**
- [ ] 能看到照片
- [ ] 点赞会变红，刷新后还在
- [ ] 评论能发，刷新后还在
- [ ] 「改名字」能用，12 个头像可切换

**收尾**
- [ ] UptimeRobot 已配置（防休眠）
- [ ] 站长面板 4 个标签页都能打开

---

*遇到问题把 `/api/health` 的返回内容截图，以及 Render 的 Logs 标签页内容发我，我来排查。*
